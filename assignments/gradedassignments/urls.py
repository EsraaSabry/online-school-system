# from rest_framework.routers import DefaultRouter
# from .views import AssignmentViewSet

# router = DefaultRouter()
# router.register(r'', AssignmentViewSet, base_name='graded-assignments')
# urlpatterns = router.urls
from django.urls import path
# from assignments.views import GradedAssignmentListView, GradedAssignmentCreateView

# urlpatterns = [
#     path('', GradedAssignmentListView.as_view()),
#     path('create/', GradedAssignmentCreateView.as_view()),
# ]
